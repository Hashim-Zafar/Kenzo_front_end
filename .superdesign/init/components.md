# Shared UI Components

The starter repository does not yet contain shared UI primitives. The only UI is the generated `app/page.tsx` starter page, so there are no reusable component source files to include at initialization time.
